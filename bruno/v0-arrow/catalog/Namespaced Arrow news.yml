info:
  name: Namespaced Arrow news
  type: websocket
  seq: 7

websocket:
  url: ws://localhost:40257/v0/arrow/{{namespace}}
  message:
    type: json
    data: "{}"
  auth: inherit

settings:
  timeout: 0
  keepAliveInterval: 0
