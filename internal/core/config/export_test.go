package config

// ConfiguredAt exposes the path-parameterised overlay read to tests.
var ConfiguredAt = configuredAt

// SaveAt exposes the path-parameterised overlay write to tests.
var SaveAt = saveAt
